#include <iostream>
using namespace std;

int main()
{
    return 0; 
} 

/*
함수와 2차원 배열
2차원 배열을 매개변수로 사용하는 함수를 작성하려면, 배열의 이름이 주소로 취급 된다는 사실을 기억해야한다.
다음과 같은 코드를 가지고 시작한다고 가정하자
int data [3][4] = {{1,2,3,4}, {9,8,7,6},{2,4,6,8}};
int total = sum(data,3);
여기서 data는 3개의 원소를 가진 배열의 이름이다. 
그것의 첫번째 원소는 그 자체가 4개의 int 값을 가지고 있는 배열이다.
data의 데이터형은 4개의 int값을 가진 배열을 지시하는 포인터이다. 
함수원형은 다음과 같다 
int sum(int (*ar2)[4], int size);
*여기서 괄호는 반드시 필요하다, 괄호를 생략하고 다음과 같이 선언하면
int *ar2[4]
4개의 int값을 가진 배열을 지시하는 포인터를 선언하는것이 아니라, int 값을 지시하는 포인터를 4개 가지고 있는 배열을 선언하는것이 된다. 
또다른 방식으로 함수 원형을 작성할수 있다.
int sum(int ar2[][4], int size);

*함수정의하는법
int sum(int ar2[][4], int size)
{
    int total = 0;
    for(int r = 0; r < size; r++)
        for(int c = 0; c < 4; c++)
            total += ar2[r][c];
    return total;
}

*/

/*

*/
