#include <iostream>
using namespace std;
inline double square(double x) {return x * x;}

int main()
{
    double a, b;
    double c = 13.0;
    a = square(5.0);
    b = square(4.5 + 7.5);
    cout << "a = " << a << ", b = " << b << endl;
    cout << "c = " << c;
    cout << ", c의 제곱 = " << square(c++) << endl;
    cout << "현재 c = " << c << endl; 
    return 0;
}

/*
C++ 인라인(inline) 함수
 - 인라인 함수는 프로그램의 실행 속도를 높이기 위해 C++에 새로 보강된 것이다.

인라인 함수를 사용하려면 다음 두 가지 작업 중에 적어도 한 가지를 반드시 해야 한다
 - 함수 선언 앞에 inline 이라는 키워드를 붙인다.
 - 함수 정의 앞에 inline 이라는 키워드를 붙인다.

인라인 함수는 함수가 수행하는 일의 강도가 낮거나 하는 일이 별로 없을때 사용하면 메모리적으로 유용하다.
대체적으로 함수안에 라인이 1줄에서 2줄인경우에 많이 사용한다.
*/

/*
a = 25, b = 144
c = 13, c의 제곱 = 169
현재 c = 14
*/
